# Local test file 
server:
  port: 8081

spring:
  application:
    name: Auth-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/authServicedb?createDatabaseIfNotExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: lhou1234

    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true

    hibernate:
      ddl-auto: create
jwt:
  issuer: test
  audience: test
